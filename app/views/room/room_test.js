'use strict';

describe('myApp.room module', function() {

  beforeEach(module('myApp.room'));

  describe('room controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('RoomCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});